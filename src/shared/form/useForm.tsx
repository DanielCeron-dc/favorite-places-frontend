import { ReactNode } from "react";

interface IFormConfig {
    

}

export const useform: [Form:ReactNode, error: string | null] = (formConfig: IFormConfig) => {
    

    return [<></> , "dsadas"] as const; 

}